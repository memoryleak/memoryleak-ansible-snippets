loop_control:
  index_var: #${1:variable name to contain the current loop index}
  loop_var: #${2:specify the name of the variable for loop}
  label: #${3:limit the displayed output}
  pause: #${4:time (in seconds) between the execution of each item }
  extended: #${5:extended loop information: Yes|No}